export interface AddressDTO {
  id?: number;
  street: string;
  city: string;
  province: string;
  postalCode: string;
  country: string;
  blockNumber: string;
  ladder: string;
  door: string;
  userId: number;
}
